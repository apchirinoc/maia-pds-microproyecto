# Dependencias extra para SERVIR inferencia real (INFERENCE_ENGINE=onnx): la API
# carga el modelo pyfunc desde S3/MLflow y ejecuta su preprocesamiento + ONNX.
# Se instalan además de requirements.txt (que ya incluye mlflow y boto3).
-r requirements.txt
onnxruntime>=1.18
opencv-python-headless>=4.9
numpy>=1.26
pandas>=2.0
Pillow>=11.0
