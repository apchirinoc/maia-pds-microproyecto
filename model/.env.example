# ---------------------------------------------------------------------------
# model — variables de entorno de PostgreSQL
# Copia este archivo a .env. El .env NO se versiona.
# ---------------------------------------------------------------------------
POSTGRES_USER=brainneuroscan
POSTGRES_PASSWORD=cambiame
POSTGRES_DB=brainneuroscan
POSTGRES_PORT=5432
